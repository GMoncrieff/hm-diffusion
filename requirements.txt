# Core DL
torch>=2.2,<3.0
torchvision>=0.17,<0.20
torchaudio>=2.2,<3.0

# Training & logging
lightning>=2.2,<3.0
wandb>=0.17

# Geospatial IO / utils
rasterio>=1.3
affine>=2.4
numpy>=1.24
tqdm>=4.66
omegaconf>=2.3

# Optional (recommended) — install if you plan to use TorchGeo datasets/transforms
# NOTE: TorchGeo pulls in extra compiled deps; prefer a Conda environment.
torchgeo>=0.5

# Dev / tests (optional)
pytest>=8.0
